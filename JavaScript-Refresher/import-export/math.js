export function add(a,b) {
  return a + b;
}

export function subtract(a,b) {
  return a-b;
}

// We use the export keyword to expose the add and 
// subtract functions. This allows other files to import and use these functions.
